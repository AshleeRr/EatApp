<div class="row mt-5">
    <div class="text-center">
        <h1>Categorias del comercio</h1>
    </div>
</div>

<div class="row">
    <div class="col-12 col-sm-4 mb-3 mt-3">
        <a href="/store/category/create" class="btn btn-dark" style="background-color: #9a3950;">Agregar
            <i class="bi bi-plus-circle"></i></a>
    </div>
</div>

<div style="max-height: 500px; overflow-y: auto;">
    <table class="table table-striped table-bordered table-hover text-center"
        style="border-radius: 12px; overflow: hidden; box-shadow: 0 2px 8px rgba(36,45,80,0.08);">
        {{#if hasCategories}}
        <div class="row">
            <thead>
                <tr>
                    <th style="background-color: #982b2b; color: white; border: none;">Nombre</th>
                    <th style="background-color: #982b2b; color: white; border: none;">Desripcion</th>
                    <th style="background-color: #982b2b; color: white; border: none;">Acciones</th>
                </tr>
            </thead>
            <tbody>
                {{#each categories}}
                <tr style="border: none; consulta-row">
                    <td style="border: none; background: #f8fafc;">{{nombre}}</td>
                    <td style="border: none; background: #f8fafc;">{{descripcion}}</td>
                    <td style="border: none; background: #f8fafc;">
                        <form action="/store/category/delete" method="POST" class="form-eliminar">
                            <a href="/store/category/edit/{{id}}" class="btn btn-warning me-2">
                                <i class="bi bi-pencil-square"></i>
                            </a>
                            <input type=" hidden" name="id" value={{id}} hidden>
                            <button type="submit" class="btn btn-danger me-2">
                                <i class="bi bi-x-circle"></i>
                            </button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
    </table>
    {{else}}
    <div class="row d-flex justify-content-center">
        <div class="col-12 col-sm-4">
            <div class="alert alert-warning" role="alert">
                <i class="bi bi-exclamation-circle-fill me-1"></i>No hay categorias disponibles
            </div>
        </div>

    </div>
    {{/if}}
</div>
</div>